spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST}/smart_home_hub?allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&&useInnoDB=true
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

mqtt:
  broker: ${MQTT_BROKER_URL}
  clientId: ${MQTT_CLIENT_ID}
  username: ${MQTT_USERNAME}
  password: ${MQTT_PASSWORD}
smart-home-hub:
  projectId: ${PROJECT_ID}
  ssoUrl: ${SSO_URL}
  excludeUri: /auth,/login,/token,/error
logging:
  level:
    root: info
    com.ryan.project.smarthomehub: debug
  file:
    path: /tmp/log